source "https://rubygems.org"

# Use the exact Jekyll version that GitHub Pages builds with
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
